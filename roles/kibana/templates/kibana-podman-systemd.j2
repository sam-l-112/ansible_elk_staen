{% set _memory_gb = ((ansible_memtotal_mb * 0.1)/1024) | int  %}
{% if _memory_gb > 8 %}
{% set _memory_gb = 8 %}
{% endif %}
### NOTICE: This file is auto-generated by ansible, DO NOT edit this file manually!

[Unit]
Description=Podman container-syslog-kibana.service
Documentation=man:podman-generate-systemd(1)
Wants=network-online.target
After=network-online.target
RequiresMountsFor=%t/containers

[Service]
Environment=PODMAN_SYSTEMD_UNIT=%n
Restart=always
TimeoutStopSec=70
ExecStartPre=/bin/rm -f %t/%n.ctr-id
ExecStart=/usr/bin/podman run \
	--cidfile=%t/%n.ctr-id \
	--cgroups=no-conmon \
	--rm \
	--sdnotify=conmon \
	--replace \
	--name=syslog-kibana \
	-d \
	-e "NODE_OPTIONS=--max-old-space-size=1024" \
	-e "LS_JAVA_OPTS=-Xms{{ _memory_gb }}g -Xmx{{ _memory_gb }}g" \
	-v /etc/hosts:/etc/hosts \
	-v /opt/syslog/kibana/syslog-kibana.yml:/usr/share/kibana/config/kibana.yml \
	--network host \
	--log-driver=journald \
	-u kibana \
	--hostname syslog-kibana \
	-m {{ _memory_gb }}g localhost/kibana:latest
ExecStop=/usr/bin/podman stop --ignore --cidfile=%t/%n.ctr-id
ExecStopPost=/usr/bin/podman rm -f --ignore --cidfile=%t/%n.ctr-id
Type=notify
NotifyAccess=all

[Install]
WantedBy=default.target
